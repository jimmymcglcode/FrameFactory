# PixelLab

Desktop приложение для экспериментов с изображениями: координатные и цветовые трансформации с предпросмотром, пресетами и экспортом.

![PixelLab](https://img.shields.io/badge/Python-3.11+-blue.svg)
![PyQt6](https://img.shields.io/badge/PyQt6-6.6+-green.svg)

## 📋 Содержание

- [Возможности](#возможности)
- [Установка](#установка)
- [Быстрый старт](#быстрый-старт)
- [Подробная инструкция](#подробная-инструкция)
- [Эффекты](#эффекты)
- [Горячие клавиши](#горячие-клавиши)
- [Структура проекта](#структура-проекта)
- [Сборка](#сборка)

## ✨ Возможности

### Геометрические эффекты
- **Shift Rows/Columns** — сдвиг строк/столбцов с плавной случайностью
- **Warp** — искажения волнами или шумовым полем
- **Block Shuffle** — перемешивание блоков изображения
- **Rotate/Flip** — поворот и отражение
- **Crop** — обрезка изображения
- **Scale** — изменение размера

### Цветовые эффекты
- **HSV Adjust** — корректировка оттенка, насыщенности и яркости
- **RGB Curves** — настройка контраста, гаммы и экспозиции
- **Channel Shuffle** — перестановка и смешивание RGB каналов
- **Posterize** — уменьшение количества цветов (постеризация)

### Эффекты деталей
- **Grain** — добавление зернистости
- **Sharpen/Blur** — увеличение резкости или размытие

### Дополнительные функции
- **Предпросмотр в реальном времени** — оптимизированный для скорости (автоматическое уменьшение до 1024px)
- **Стек эффектов** с возможностью включения/выключения
- **Изменение порядка эффектов** перетаскиванием (drag & drop)
- **Сохранение и загрузка пресетов** (JSON формат)
- **Генерация случайных пресетов** для быстрых экспериментов
- **Undo/Redo система** — отмена и повтор действий
- **Экспорт в PNG, JPEG, WebP** с настройками качества
- **Темная тема интерфейса** — комфортная работа в любое время суток
- **Индикатор загрузки с прогрессом**:
  - Отображение прошедшего времени обработки
  - Примерное оставшееся время (на основе реальной калибровки)
  - Процент выполнения операции
  - Кнопка отмены для прерывания обработки
- **Умная оценка времени** — автоматическая калибровка производительности
- **Асинхронная обработка** — интерфейс не зависает во время операций
- **Логирование событий и ошибок** с отображением в интерфейсе

## 🚀 Установка

### Требования
- Python 3.11 или выше
- Windows (обязательно), macOS/Linux (опционально)

### Шаг 1: Клонирование репозитория
```bash
git clone <repository-url>
cd pixel-lab
```

### Шаг 2: Создание виртуального окружения (рекомендуется)
```bash
# Windows
python -m venv .venv
.venv\Scripts\activate

# macOS/Linux
python3 -m venv .venv
source .venv/bin/activate
```

### Шаг 3: Установка зависимостей
```bash
pip install -r requirements.txt
```

Или установка вручную:
```bash
pip install PyQt6>=6.6.0 Pillow>=10.0.0 opencv-python>=4.8.0 numpy>=1.24.0
```

## 🎯 Быстрый старт

1. **Запустите приложение:**
   ```bash
   python app/main.py
   ```

2. **Откройте изображение:**
   - Нажмите `Ctrl+O` или кнопку "Open"
   - Выберите файл (PNG, JPG, WebP, BMP, TIFF)

3. **Примените эффект:**
   - Выберите категорию эффектов во вкладках справа
   - Нажмите на название эффекта
   - Настройте параметры
   - Нажмите "Apply"

4. **Сохраните результат:**
   - Нажмите `Ctrl+S` или кнопку "Save"
   - Выберите формат и место сохранения

## 📖 Подробная инструкция

### Интерфейс приложения

Приложение состоит из нескольких основных областей:

1. **Верхняя панель (Toolbar)**:
   - **Open** — открыть изображение (`Ctrl+O`)
   - **Save** — сохранить обработанное изображение (`Ctrl+S`)
   - **Reset** — сбросить все эффекты и вернуться к оригиналу
   - **Undo** — отменить последнее действие (`Ctrl+Z`)
   - **Redo** — повторить отмененное действие (`Ctrl+Y`)
   - **Zoom** — уровень масштабирования (Fit, 100%, 200%)

2. **Область просмотра изображений** (слева):
   - **Original** — оригинальное изображение (верхняя панель)
   - **Preview** — предпросмотр результата после применения эффектов (нижняя панель)

3. **Панель эффектов** (справа):
   - **Вкладки категорий**: Geometry, Color, Detail
   - **Список эффектов** — выбор эффекта для применения
   - **Параметры эффекта** — настройка выбранного эффекта
   - **Кнопки**: "Randomize" (случайные параметры), "Apply" (применить)

4. **Стек эффектов** (справа, ниже):
   - Список примененных эффектов с чекбоксами включения/выключения
   - Кнопки удаления (×) для каждого эффекта
   - Кнопка "Clear All" для очистки всего стека

5. **Индикатор загрузки**:
   - Появляется автоматически во время обработки
   - Показывает прошедшее время и оставшееся время
   - Отображает процент выполнения
   - Кнопка "Отмена" для прерывания обработки

6. **Строка состояния** (внизу):
   - Размер изображения
   - Формат файла
   - Время последней операции

### Главное окно

Приложение состоит из нескольких основных областей:

#### Левая панель — Просмотр изображений
- **Original** (верх) — показывает исходное изображение без изменений
- **Preview** (низ) — показывает результат применения эффектов

#### Правая панель — Управление эффектами
- **Вкладки эффектов** — категории: Geometry, Color, Detail
- **Панель параметров** — настройка выбранного эффекта
- **Стек эффектов** — список применённых эффектов

#### Верхняя панель — Меню и инструменты
- **File** — работа с файлами и пресетами
- **Edit** — отмена/повтор действий
- **View** — настройки отображения

#### Нижняя панель — Статус-бар
- Размер изображения
- Формат файла
- Время обработки

### Работа с изображениями

#### Открытие изображения
1. **Меню:** File → Open (`Ctrl+O`)
2. **Toolbar:** Кнопка "Open"
3. **Поддерживаемые форматы:** PNG, JPG, JPEG, WebP, BMP, TIFF

#### Сохранение изображения
1. **Меню:** File → Save As (`Ctrl+S`)
2. **Toolbar:** Кнопка "Save"
3. **Форматы:** PNG, JPEG, WebP
4. **Примечание:** При сохранении эффекты применяются к полному разрешению изображения

### Работа с эффектами

#### Применение эффекта
1. Выберите категорию эффектов (Geometry/Color/Detail)
2. Нажмите на название эффекта
3. В панели параметров настройте значения
4. Нажмите кнопку **"Apply"** для добавления в стек

#### Настройка параметров
- **SpinBox** (числовые значения) — используйте стрелки или введите значение
- **DoubleSpinBox** (дробные значения) — для параметров с десятичными значениями
- **ComboBox** (выбор из списка) — выпадающий список опций
- **CheckBox** (да/нет) — включение/выключение опции

#### Кнопка "Randomize"
- Случайно изменяет все параметры эффекта в безопасных диапазонах
- Используется для экспериментов и поиска интересных комбинаций

#### Стек эффектов
- **Чекбокс** — включить/выключить эффект (без удаления)
- **Перетаскивание** — изменить порядок применения эффектов
- **Кнопка "×"** — удалить эффект из стека
- **Кнопка "Clear All"** — очистить весь стек

**Важно:** Порядок эффектов имеет значение! Один и тот же набор эффектов в разном порядке даст разный результат.

### Масштабирование

В toolbar доступны три режима масштабирования:
- **Fit** — автоматическое масштабирование под размер окна
- **100%** — реальный размер изображения
- **200%** — увеличение в 2 раза

### Пресеты

#### Сохранение пресета
1. Примените нужные эффекты
2. Меню: File → Save Preset
3. Выберите место сохранения
4. Файл сохранится в формате JSON

#### Загрузка пресета
1. Меню: File → Load Preset
2. Выберите файл пресета (.json)
3. Эффекты применятся автоматически

#### Случайный пресет
1. Меню: File → Random Preset
2. Применяется случайный набор из 1-3 эффектов
3. Параметры генерируются случайно

**Формат пресета:**
```json
{
  "version": "1.0",
  "pipeline": {
    "effects": [
      {
        "class": "Warp",
        "params": {
          "type": "wave",
          "amount": 15.0,
          "scale": 50.0,
          "seed": 42
        },
        "enabled": true
      }
    ]
  }
}
```

### Undo/Redo

- **Отменить:** `Ctrl+Z` или кнопка "Undo"
- **Повторить:** `Ctrl+Y` или кнопка "Redo"
- **История:** До 50 действий

### Сброс

- **Меню:** Edit → Reset
- **Toolbar:** Кнопка "Reset"
- Удаляет все эффекты и возвращает к исходному изображению

## 🎨 Эффекты

### Geometry (Геометрия)

#### Shift Rows/Columns
Сдвигает строки или столбцы изображения с плавной случайностью.

**Параметры:**
- **Direction** — направление: `rows` (строки), `columns` (столбцы), `both` (оба)
- **Max Shift** — максимальный сдвиг в пикселях (0-1000)
- **Smoothness** — плавность изменения (0.0-1.0): чем выше, тем плавнее переход
- **Seed** — зерно для генератора случайных чисел (0-2147483647)
- **Wrap Mode** — режим обработки краёв:
  - `wrap` — заворачивание (циклический сдвиг)
  - `reflect` — отражение
  - `clamp` — закрепление краёв

#### Warp
Искажает изображение волнами или шумовым полем через обратное отображение.

**Параметры:**
- **Type** — тип искажения: `wave` (волны), `noise` (шум)
- **Amount** — сила смещения в пикселях (0.0-100.0)
- **Scale** — размер волн/шума (0.0-1000.0): меньше = более частые волны
- **Angle** — угол направления волн (0-360 градусов)
- **Seed** — зерно для генератора (0-2147483647)
- **Interpolation** — метод интерполяции:
  - `nearest` — ближайший пиксель (быстро, но грубо)
  - `bilinear` — билинейная (баланс скорости и качества)
  - `bicubic` — бикубическая (медленно, но качественно)

#### Block Shuffle
Перемешивает мозаику блоков, сохраняя узнаваемость изображения.

**Параметры:**
- **Block Size** — размер блока в пикселях (рекомендуется ≥16)
  - ⚠️ **Внимание**: Маленькие значения (1-4) значительно увеличивают время обработки
  - Для изображения 1024×1024px с `block_size=1` обработка может занять несколько минут
  - Рекомендуется использовать `block_size` ≥ 16 для оптимальной производительности
- **Shuffle Strength** — доля блоков для перемешивания (0.0-1.0)
- **Block Transform** — трансформация внутри блока:
  - `none` — без изменений
  - `rotate` — поворот на 90/180/270°
  - `flip` — отражение (горизонтальное или вертикальное)
  - `jitter` — небольшое смещение
- **Seed** — зерно для генератора (0-2147483647)

#### Rotate/Flip
Поворачивает и/или отражает изображение.

**Параметры:**
- **Rotation** — угол поворота: 0°, 90°, 180°, 270°
- **Flip Horizontal** — отражение по горизонтали (да/нет)
- **Flip Vertical** — отражение по вертикали (да/нет)

#### Crop
Обрезает изображение по заданным координатам.

**Параметры:**
- **X** — начальная координата X (0-100% или пиксели)
- **Y** — начальная координата Y (0-100% или пиксели)
- **Width** — ширина области (0-100% или пиксели)
- **Height** — высота области (0-100% или пиксели)
- **Mode** — режим: `percent` (проценты), `absolute` (пиксели)

#### Scale
Изменяет размер изображения.

**Параметры:**
- **Scale X** — масштаб по горизонтали в процентах (1-1000%)
- **Scale Y** — масштаб по вертикали в процентах (1-1000%)
- **Interpolation** — метод интерполяции:
  - `nearest` — ближайший пиксель
  - `bilinear` — билинейная
  - `bicubic` — бикубическая
  - `lanczos` — Ланцош (лучшее качество)

### Color (Цвет)

#### HSV Adjust
Корректирует оттенок, насыщенность и яркость изображения.

**Параметры:**
- **Hue Shift** — сдвиг оттенка (-180 до 180): 0 = без изменений, ±180 = противоположный цвет
- **Saturation** — насыщенность (0.0-2.0): 0 = чёрно-белое, 1 = норма, >1 = усиление
- **Value** — яркость (0.0-2.0): 0 = чёрное, 1 = норма, >1 = осветление

#### RGB Curves
Корректирует контраст, гамму и экспозицию.

**Параметры:**
- **Contrast** — контрастность (-100 до 100): отрицательные значения уменьшают, положительные увеличивают
- **Gamma** — гамма-коррекция (0.2-3.0): <1 = осветление теней, >1 = затемнение
- **Exposure** — экспозиция (-2.0 до 2.0): отрицательные значения затемняют, положительные осветляют

#### Channel Shuffle
Переставляет каналы RGB и смешивает их.

**Параметры:**
- **Mode** — режим перестановки:
  - `rgb` — без изменений
  - `rbg`, `grb`, `gbr`, `brg`, `bgr` — различные перестановки
  - `mix` — смешивание каналов
- **Mix Amount** — сила смешивания (0.0-1.0, только для режима `mix`)

#### Posterize
Уменьшает количество цветов (постеризация).

**Параметры:**
- **Levels** — количество уровней цвета (2-256): меньше = более выраженный эффект
- **Dither** — дизеринг (да/нет): сглаживание переходов между цветами

### Detail (Детали)

#### Grain
Добавляет зернистость (шум) к изображению.

**Параметры:**
- **Amount** — количество зерна (0.0-1.0): 0 = нет эффекта, 1 = максимальный эффект
- **Size** — размер зерна (1-5): больше = более крупное зерно
- **Monochrome** — монохромное зерно (да/нет): если включено, зерно одинаковое для всех каналов

#### Sharpen/Blur
Увеличивает резкость или размывает изображение.

**Параметры:**
- **Mode** — режим: `blur` (размытие), `sharpen` (резкость)
- **Blur Sigma** — сила размытия (0.0-10.0, для режима blur): больше = сильнее размытие
- **Sharpen Amount** — сила резкости (0.0-2.0, для режима sharpen): больше = более резкое изображение

## ⌨️ Горячие клавиши

### Основные команды
- `Ctrl+O` — Открыть изображение
- `Ctrl+S` — Сохранить изображение
- `Ctrl+Z` — Отменить последнее действие (Undo)
- `Ctrl+Y` — Повторить отмененное действие (Redo)
- `Ctrl+Shift+S` — Сохранить пресет
- `Ctrl+Shift+O` — Загрузить пресет
- `F1` — Показать справку
- `Esc` — Закрыть диалоговое окно

### Во время обработки
- Кнопка "Отмена" в индикаторе загрузки — прервать обработку

## 📁 Структура проекта

```
pixel-lab/
├── app/
│   ├── main.py              # Точка входа в приложение
│   ├── core/                # Основные модули
│   │   ├── image_model.py   # Модель изображения
│   │   ├── pipeline.py      # Стек эффектов
│   │   ├── preset.py        # Пресеты
│   │   └── logger.py        # Логирование
│   ├── effects/             # Эффекты
│   │   ├── base.py          # Базовый класс
│   │   ├── shift.py         # Shift rows/columns
│   │   ├── warp.py          # Warp эффекты
│   │   ├── block_shuffle.py # Block shuffle
│   │   ├── transform.py    # Rotate/Flip/Crop/Scale
│   │   ├── color.py         # Цветовые эффекты
│   │   ├── grain.py         # Grain и Sharpen/Blur
│   │   └── registry.py     # Регистр эффектов
│   └── ui/                  # Пользовательский интерфейс
│       ├── main_window.py   # Главное окно
│       ├── image_viewer.py  # Просмотрщик изображений
│       ├── effect_panel.py  # Панель эффектов
│       ├── effect_stack.py  # Стек эффектов
│       ├── log_panel.py     # Панель логов
│       ├── loading_overlay.py # Индикатор загрузки с прогрессом
│       ├── help_dialog.py   # Диалог справки
│       └── theme.py         # Темная тема
├── requirements.txt         # Зависимости
└── README.md               # Документация
```

## 🔧 Сборка

### Сборка исполняемого файла (PyInstaller)

1. Установите PyInstaller:
   ```bash
   pip install pyinstaller
   ```

2. Создайте spec файл (опционально):
   ```bash
   pyinstaller --name PixelLab app/main.py
   ```

3. Соберите onefile версию:
   ```bash
   pyinstaller --onefile --windowed --name PixelLab app/main.py
   ```

4. Исполняемый файл будет в папке `dist/`

### Параметры сборки

- `--onefile` — один исполняемый файл
- `--windowed` — без консоли (только для Windows)
- `--name PixelLab` — имя исполняемого файла
- `--icon=icon.ico` — иконка приложения (если есть)

## 🐛 Решение проблем

### Модуль не найден
Если возникает ошибка `ModuleNotFoundError`:
1. Убедитесь, что виртуальное окружение активировано
2. Установите зависимости: `pip install -r requirements.txt`

### Медленная работа

- **Предпросмотр** автоматически уменьшается до 1024px по длинной стороне для быстрой обработки
- **При сохранении** применяется полное разрешение — это может занять значительно больше времени
- **Block Shuffle с маленьким block_size** (1-4) очень медленный:
  - Для изображения 1024×1024px с `block_size=1` может потребоваться несколько минут
  - Используйте `block_size` ≥ 16 для приемлемой скорости
  - Если обработка занимает слишком много времени, используйте кнопку "Отмена"
- **Большие изображения** (4K+) требуют больше времени для обработки
- **Множественные эффекты** — чем больше эффектов в стеке, тем дольше обработка

### Ошибки при сохранении
- Проверьте права доступа к папке
- Убедитесь, что достаточно места на диске
- Проверьте формат файла в диалоге сохранения

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад

Предложения по улучшению приветствуются! Создайте issue или pull request.

## 📧 Контакты

Для вопросов и предложений создайте issue в репозитории проекта.

---

**Приятной работы с PixelLab! 🎨**
